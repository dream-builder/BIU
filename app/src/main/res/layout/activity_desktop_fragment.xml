<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_desktop_fragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    tools:context="com.bitsofttech.biu.Desktop_fragment">


        <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:src="@drawable/biu_building"
            android:scaleType="centerCrop"
            android:layout_marginBottom="25dp"
            android:id="@+id/desktopImageView"
            />

        <GridView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/iconGridView"
            android:layout_below="@id/desktopImageView"
            android:numColumns="3"
            >
        </GridView>

</RelativeLayout>